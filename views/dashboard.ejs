<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bank</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
</head>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"
    integrity="sha384-oBqDVmMz9ATKxIep9tiCxS/Z9fNfEXiDAYTujMAeBAsjFuCZSmKbSSUnQlmh/jp3"
    crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.min.js"
    integrity="sha384-cuYeSxntonz0PPNlHhBs68uyIAVpIIOZZ5JqeqvYYIcEL727kskC66kF92t6Xl2V"
    crossorigin="anonymous"></script>


<body>
    <div id="navbar" class="container">
        <div class="container">
            <nav class="navbar navbar-expand-lg bg-light">
                <div class="container-fluid">
                    <a class="navbar-brand" href="#">
                        <img class="logo" src="/img/logo.png" alt="">
                    </a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                        <ul class="navbar-nav mx-auto container-fluid justify-content-around">
                            <li class="nav-item text-center w-25 mx-3">
                                <a class="nav-link" aria-current="page" href="/">Home</a>
                            </li>
                            <li class="nav-item text-center w-25 mx-3">
                                <a class="nav-link" href="#services">Services</a>
                            </li>
                            <li class="nav-item text-center w-25 mx-3">
                                <a class="nav-link" href="#">About us</a>
                            </li>
    
                            <!-- Conditionally render buttons based on whether user is logged in -->
                            <% if (userId) { %>
                                <li class="nav-item text-center w-25 mx-3">
                                    <a class="nav-link" href="/logout">Logout</a>
                                </li>
                            <% } else { %>
                                <li class="nav-item text-center w-25 mx-3">
                                    <a class="nav-link" href="/login">Sign in</a>
                                </li>
                                <li class="nav-item text-center w-25 mx-3">
                                    <a class="nav-link" href="/register">Register</a>
                                </li>
                            <% } %>
                        </ul>
                    </div>
                </div>
            </nav>
        </div>
    </div>

    



    <div  class="container">


        <div id="dashboard" class="container">
            
            <h1>Welcome to Your Dashboard</h1>
            <button id="openCardBtn">Open Card</button>

            <div id="cardInfo" style="display:none;">
                <h2>Your Card Details</h2>
                <button id="showDetailsBtn" class="btn btn-primary mb-3">Show Details</button>

            </div>

            <!-- Gold Card -->
            <div id="bankCard" class="col-md-4 mb-4">
                <div class="credit-card bg-warning text-dark p-4 d-flex flex-column justify-content-between">
                    <div class="d-flex justify-content-between align-items-center">
                        <h4><img class="logo me-2" id="cardLogo" src="/img/logo.png" alt="">Bank Card</h4>

                    </div>
                    <div class="d-flex justify-content-between align-items-center">
                        <div id="cardNumber" class="card-number my-3">
                            **** **** **** 5678
                        </div>
                        <div>
                            cvv: <span id="cvv">***</span>
                        </div>
                    </div>

                    <div class="d-flex justify-content-between">
                        <div>
                            <div id="cardholderName" class="card-holder"></div>
                            <div id="expiryDate"></div>
                        </div>
                        <img src="https://www.svgrepo.com/show/362035/visa-3.svg" alt="Chip" width="40">
                    </div>
                </div>
            </div>

        </div>

    </div>



    <div id="footer" class="container-fluid">
        <div class="row d-flex"> <!-- Added text-center for center alignment -->
            <div class="col d-flex align-items-center justify-content-center">
                <img class="iconImg" src="/img/facebook.png" alt="">
                <img class="iconImg" src="/img/instagram.png" alt="">
                <img class="iconImg" src="/img/linkedin.png" alt="">
                <img class="iconImg" src="/img/youtube.png" alt="">
            </div>

        </div>
        <div class="row d-flex align-items-center justify-content-center pb-3">

            © 2024 Astana Kazakhstan. All rights reserved.

        </div>
    </div>


    <script>
        async function loadCard() {
            try {
                // Fetch card details from the server
                const response = await fetch('/user-card', {
                    method: 'GET',
                    credentials: 'include' // Include cookies for session handling
                });

                if (!response.ok) {
                    throw new Error('Failed to load card');
                }

                const cardDetails = await response.json();
                const expiryDate = new Date(cardDetails.expiryDate);

                // Format card number to show only the last four digits initially
                const maskedCardNumber = `**** **** **** ${cardDetails.cardNumber.slice(-4)}`;
                document.getElementById('cardNumber').innerText = maskedCardNumber;
                document.getElementById('cardholderName').innerText = cardDetails.cardholderName;
                document.getElementById('expiryDate').innerText = `${String(expiryDate.getMonth() + 1).padStart(2, '0')}/${String(expiryDate.getFullYear()).slice(2)}`;
                document.getElementById('cvv').innerText = '***';

                // Show the card information section and hide the "Open Card" button if the card exists
                document.getElementById('cardInfo').style.display = 'block';
                document.getElementById('openCardBtn').style.display = 'none';

                // Toggle button functionality
                const showDetailsBtn = document.getElementById('showDetailsBtn');
                let detailsVisible = false;

                showDetailsBtn.addEventListener('click', () => {
                    if (detailsVisible) {
                        // Hide details
                        document.getElementById('cardNumber').innerText = maskedCardNumber;
                        document.getElementById('cvv').innerText = '***';
                        showDetailsBtn.innerText = 'Show Details';
                    } else {
                        // Show full card number and CVV
                        document.getElementById('cardNumber').innerText = cardDetails.cardNumber.match(/.{1,4}/g).join(" ");
                        document.getElementById('cvv').innerText = cardDetails.cvv;
                        showDetailsBtn.innerText = 'Hide Details';
                    }
                    detailsVisible = !detailsVisible;
                });
            } catch (error) {
                console.error(error);
                document.getElementById("bankCard").style.display = 'none';
            }
        }

        // Call loadCard function when the page loads
        window.onload = loadCard;

    </script>
</body>

</html>